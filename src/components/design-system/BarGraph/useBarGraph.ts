/*
  what data does the useBarGraph need ?
  =====================================
  • {min} & {max} values
  • {dataPoints} to define the divisions
*/

import { UseBarGraphProps } from './types'

export const useBarGraph = ({ min, max, data }: UseBarGraphProps) => {}
